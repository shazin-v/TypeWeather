/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['openweathermap.org'], // Add openweathermap.org to allowed domains
    },
  };
  
  export default nextConfig;
  